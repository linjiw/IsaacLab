[pytest]
# The vendored run-manager core is pure-Python and CPU-only. Tests inject
# their own registry spec (the core ships no default action space) and add
# the package root to sys.path via tests/conftest.py.
testpaths = tests
addopts = -q -rs
